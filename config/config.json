{
  "development": {
    "username": "${DATABASE_USER}",
    "password": "${DATABASE_PASSWORD}",
    "database": "${DATABASE_NAME}",
    "host": "${DATABASE_HOST}",
    "dialect": "postgres"
  },
  "test": {
    "username": "${DATABASE_USER}",
    "password": "${DATABASE_PASSWORD}",
    "database": "${DATABASE_NAME}",
    "host": "${DATABASE_HOST}",
    "dialect": "postgres"
  },
  "production": {
    "username": "${DATABASE_USER}",
    "password": "${DATABASE_PASSWORD}",
    "database": "${DATABASE_NAME}",
    "host": "${DATABASE_HOST}",
    "dialect": "postgres"
  }
}
